<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- тот же экран, но собранный на тёмной основе: иначе окно под ним остаётся белым -->
    <style name="Theme.QRChecker" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
